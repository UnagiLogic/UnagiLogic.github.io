// This is the splash screen module
// It is responsible for showing the splash screen
// and loading the game assets
// It also initializes the game state and starts the game loop
// It exports a function that initializes the splash screen to app.js
// It contains the logo 

/*
function splashScreen() {
    // Load the game assets
    gameAssets.loadAssets(function() {
        // Initialize the game state
        gameState.init();

        // Start the game loop
        gameLoop.start();
    });
}

// Import the game module
import { game } from './game.js';

// Import the game state module
import { gameState } from './game-state.js';

// Import the game loop module
import { gameLoop } from './game-loop.js';

// Import the game assets module
import { gameAssets } from './game-assets.js';

// Import the game constants module
import { gameConstants } from './game-constants.js';

// Import the game utils module
import { gameUtils } from './game-utils.js';

// Import the game audio module
import { gameAudio } from './game-audio.js';

// Import the game input module
import { gameInput } from './game-input.js';

// Import the game physics module
import { gamePhysics } from './game-physics.js';

// Import the game render module
import { gameRender } from './game-render.js';

// Import the game score module
*/

export function sayHelloAgain() {
    console.log('Hello again!');
}
